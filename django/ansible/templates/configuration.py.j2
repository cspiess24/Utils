'''
Make a copy of this file and update configuration with the required
information for your environment
'''

# This should be the FQDN you are using to access NCP
# https://docs.djangoproject.com/en/2.0/ref/settings/#allowed-hosts
ALLOWED_HOSTS = ["*"]

# Database
# https://docs.djangoproject.com/en/2.0/ref/settings/#databases
# This project was built around Postgresql, enter postgres connection information
DATABASE = {
    'ENGINE': 'django.db.backends.postgresql',
    'NAME': 'ncp',
    'USER': 'django',
    'PASSWORD': "{{ lookup('file', '/tmp/psql_password') }}",
    'HOST': 'localhost',
    'PORT': '',
}

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = "{{ lookup('password', '/tmp/secret_key chars=ascii_letters,digits,hexdigits,punctuation') }}"

# Specify one or more name and email address tuples representing administrators.
# These people will be notified of application errors
# assuming correct email settings are provided.
# https://docs.djangoproject.com/en/2.0/ref/settings/#admins
ADMINS = [
    # ('John', 'jdoe@example.com'),
    # ('Mary', 'mary@example.com'),
]

# Email settings
EMAIL = {
    'SERVER': 'localhost',
    'PORT': 25,
    'USERNAME': '',
    'PASSWORD': '',
    'TIMEOUT': 10,
    'FROM_EMAIL': '',
}

CSRF_COOKIE_SECURE = False
DEBUG = True
SESSION_COOKIE_SECURE = False
TIME_ZONE = "{{ time_zone | default('UTC') }}"